# security (leave blank during dev if you don't want auth enforced)
JWT_SECRET=

# kafka bootstrap for all services
KAFKA_BOOTSTRAP=kafka:9092

# notifier's prometheus exporter
PROM_PORT=8008

# (only needed if you enable JWT) a dev token for the frontend WS
WS_TOKEN=
